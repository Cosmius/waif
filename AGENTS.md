## Formatting Rules

- Keep all lines under 80 characters where possible. This is a soft rule,
  follow it when it does not make the text harder to write or read (e.g. table
  cells, URLs, and code blocks may exceed the limit).
